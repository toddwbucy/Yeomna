[package]
name = "late-loop"
version = "0.1.0"
edition = "2024"

[dependencies]
yeomna-chunking = { path = "../../../crates/yeomna-chunking" }
serde_json = "1"
sha2 = "0.10"

# Deliberately its own workspace: this is a spike harness, not a member crate.
[workspace]
